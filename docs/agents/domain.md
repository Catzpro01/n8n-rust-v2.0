# Domain docs

This repository currently uses a single context. Before work, read root `CONTEXT.md` and relevant decisions under `docs/adr/`. Use glossary terms in tickets, tests, code, and review. If work contradicts an ADR, surface the conflict and supersede it explicitly rather than silently drifting.
